apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  labels:
    Project: roboshop
    component: mysql
    tier: db 
  namespace: roboshop
spec:
  replicas: 1
  selector:
    matchLabels:
      Project: roboshop
      component: mysql
      tier: db
  template:
    metadata:
      labels:
        Project: roboshop
        component: mysql
        tier: db
    spec:
      containers:
      - name: mysql
        image: akhilln/mysql:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 3306
---
apiVersion: v1 
kind: Service
metadata:
  name: mysql
spec:
  selector:
    Project: roboshop
    component: mysql
    tier: db
  ports:
  - protocol: TCP
    port: 3306 # service port
    targetPort: 3306 # contaner port
      